{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<table class="table">
  <thead class="blue-gradient">
    <tr>
      <th scope="col">Codigo</th>
      <th scope="col">Nome</th>
      <th scope="col">ID</th>
      <th scope="col">Notas</th>
    </tr>
  </thead>
  <tbody>
    {{#each materias}}
    <tr>
      <th scope="row">{{codigo}}</th>
      <td>{{titulo}}</td>
      <td>{{_id}}</td>
      <td><button class="btn btn-success mt-2" type="submit">
          <i class="far fa-check-circle"> Lançar Nota</i></td>
    </tr>
    {{else}}
    <p>Nenhuma materias registrada!</p>
    {{/each}}
  </tbody>
</table>