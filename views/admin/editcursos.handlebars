{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}

{{/each}}

<h3>Editar Curso:</h3>
<div class="card">
  <div class="card-body">
    <form action="/admin/cursos/edit" method="POST">
      <input type="hidden" name="id" value="{{curso.id}}">
      <label for="nome">Nome: </label>
      <input type="text" id="nome" name="nome" placeholder="Nome da Curso" class="form-control" value="{{curso.nome}}">
      <label for="slug">Slug: </label>
      <input type="text" id="slug" name="slug" placeholder="Slug da curso" class="form-control" value="{{curso.slug}}">
      <label for="descricao">Descrição do curso: </label>
      <textarea id="descricao" name="descricao" class="form-control value="
        {{curso.descricao}}"">{{curso.descricao}}</textarea>
      <br>
      <button type="submit" class="btn btn-success">Editar curso</button>
    </form>
  </div>
</div>