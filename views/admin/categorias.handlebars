<h2>Lista de Cursos:</h2>
<hr>
<a href="/admin/categorias/add"><button type="button" class="btn btn-success">Nova categoria</button></a>

{{#each categorias}}
<div class="card mt-4">
  <div class="card-body">
    <h4>{{nome}}</h4>
    <small>Slug: {{slug}}</small><br>
    <small>Data de criação: {{date}}</small><br>
    <a href="/admin/categorias/edit/{{_id}}"><button type="button"
        class="btn btn-outline-success mt-4">Editar</button></a>
    <form action="/admin/categorias/deletar/" method="POST">
      <input type="hidden" name="id" value="{{_id}}">
      <button type="submit" class="btn btn-outline-danger mt-2">Deletar</button>
    </form>
  </div>
</div>
{{else}}
<h4 class="mt-3">Nenhuma categoria registrada :( </h4>
{{/each}}